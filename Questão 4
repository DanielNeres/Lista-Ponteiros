#include <stdio.h>
#include <math.h>
int main()
{
	float area = 0, lado = 2, perimetro = 0;
	printf("%f\t%f\n", lado, area);
	area = ((3 * (lado*lado)) * sqrt(3))/2;
	printf("%f", area);
		return 0;
}
